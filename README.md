# Halation mockup

Clickable prototype of Halation, one place to run a camera business.

- `index.html` is the landing page.
- `app.html` opens straight into the demo app.
- `changelog.html` lists every update, newest first.

Everything here is sample data: the clients, people and numbers are illustrative. Built from the private source repo with `mockup/publish.sh`.
